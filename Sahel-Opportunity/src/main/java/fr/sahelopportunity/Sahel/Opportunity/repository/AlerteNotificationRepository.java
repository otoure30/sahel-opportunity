package fr.sahelopportunity.Sahel.Opportunity.repository;

import org.springframework.data.jpa.repository.JpaRepository;

// AlerteNotificationRepository
public interface AlerteNotificationRepository<AlerteNotification> extends JpaRepository<AlerteNotification, Integer> {
}